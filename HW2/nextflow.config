profiles {
    conda      { conda.enabled  = true  }
}

process {
    cpus   = 16
    memory = '16.GB'

    withName: 'SPADES'    { cpus = 16; memory = '16.GB' }
    withName: 'MAP_READS' { cpus = 16 }
}
